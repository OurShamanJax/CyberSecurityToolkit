# R.O.D.E v4 — backend dependencies (all free/open-source, local)
fastapi>=0.111
uvicorn[standard]>=0.34
wsproto>=1.2               # version-stable WebSocket backend
sqlalchemy>=2.0
pydantic>=2.0
pyyaml>=6.0
aiofiles>=23.0
requests>=2.28
psutil>=5.9                # running-process list (Analyzer)
cryptography>=41.0         # X25519 keys for the WireGuard VPN generator

# dev / testing
pytest>=8.0
